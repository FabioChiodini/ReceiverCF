---
applications:
- name: receiver-${random-word}
  memory: 128M
  buildpack: python_buildpack
  domain: cfapps.io
  command: python receiver.py
  timeout: 20
  
  #Added this K
  # command to install dependencies
  #install: "pip install -r requirements.txt"
  #env:
  #  LOG_HOST: 192.168.0.1
  #  LOG_PORT: 61112
